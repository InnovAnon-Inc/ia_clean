services:
  ia_clean:
    hostname:         ia_clean.innovanon.com
    build:
      context:        .
      tags:
        - innovanon/ia_clean:latest
        -           ia_clean:latest
    restart:          no
    logging:
      driver:         syslog   
